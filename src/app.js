import React, { Fragment } from "react";
import { render } from "react-dom";

function App() {
	return (
		<Fragment>
			<h1>GitFit</h1>
		</Fragment>
	);
}

render(<App />, document.getElementById("root"));
